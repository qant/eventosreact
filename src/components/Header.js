import React from "react";

const Header = () => (
  <header className="uk-margin" ul-margin="true">
    <h1 className="uk-text-center">Events with React with EventBriteAPI</h1>
  </header>
);

export default Header;
